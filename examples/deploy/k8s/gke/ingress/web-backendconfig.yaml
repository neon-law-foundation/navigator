apiVersion: cloud.google.com/v1
kind: BackendConfig
metadata:
  name: navigator-web
  namespace: navigator
spec:
  customRequestHeaders:
    headers:
      - "X-Navigator-Client-Region:{client_region}"
