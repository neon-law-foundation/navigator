coverage:
  status:
    project:
      default:
        target: auto
        threshold: 0%
        if_not_found: failure
        informational: false
    patch:
      default:
        target: 100%
        threshold: 0%
        if_not_found: failure
        informational: false
