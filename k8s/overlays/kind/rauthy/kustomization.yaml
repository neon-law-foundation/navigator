apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local-only identities and client registration. Any non-KIND environment
# supplies its own `rauthy-secrets`, `rauthy-client`, and `rauthy-bootstrap`.
resources:
  - local-fixture.yaml
