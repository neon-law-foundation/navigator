interface:
  display_name: "Cut Release"
  short_description: "Run the release preflight, write the version, land it, and tag the merged commit."
  default_prompt: "Use $cut-release to run the release preflight, write the workspace version, land it through a PR, and tag the merged commit."
