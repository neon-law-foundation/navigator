# CODEOWNERS — @nick reviews everything.
#
# The handle must resolve on the host this repository lives on. This enterprise
# is GitHub Enterprise Cloud with data residency, whose accounts are
# EMU-provisioned through Google SAML and share no namespace with github.com:
# the github.com handle `@shicholas` does not exist here, and GitHub silently
# ignores an owner it cannot resolve. An unresolvable owner does not fail the
# file — it just leaves every path unowned, which is indistinguishable from
# having no CODEOWNERS at all. `ops github setup` resolves every owner named
# here against the API for exactly that reason.
#
# https://docs.github.com/articles/about-code-owners
* @nick
